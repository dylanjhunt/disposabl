<div class="slidershow">
</div>

<div class="container">
  <h1 class="allcosts">Your Disposable Income! - $<%= Income.where(user_id: @current_user.id).sum('amount') - Cost.where(user_id: @current_user.id).sum('amount') %>/month</h1>
</div>
<% if (Income.where(user_id: @current_user.id).sum('amount') - Cost.where(user_id: @current_user.id).sum('amount')) > 200 %>
<div class="container gif">
  <h2> Your Income is over $1000!</h2>
  <img src="http://gifsec.com/wp-content/uploads/GIF/2014/03/Happy-Success-GIF.gif?gs=a">
</div>
<% end %>

<% if (Income.where(user_id: @current_user.id).sum('amount') - Cost.where(user_id: @current_user.id).sum('amount')) < 500 %>
<div class="container gif">
  <h2> Your disposable income is under $200 :(</h2>
  <img src="https://media.giphy.com/media/qPuA1KdbOt6QU/giphy.gif">
</div>
<% end %>
